
// Must manually find out how many bytes are inside the string that will be added to the buffer
char buffer_insert(char& buffer, char& string_insert, std::size_t amt)
{
	for (int i = 0; i < amt; ++i)
	{
		while ()

		std::cout << "Bytes in current string: " << bytes << std::endl;

		for (int i = 0; i < max_bytes; ++i)
		{
			buffer[i] = string_insert
		}
	}
	
}

std::size_t find_row_bytes(char& string, std::size_t max)
{
	short count;

	for (int i = 0; i < max; ++i)
	{
		if (string[i] == '\o')
			break;

		count++;
	}

	return count;
}

char buffer_parse(char& buffer)
{

}